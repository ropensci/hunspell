PKG_CXXFLAGS=
PKG_CPPFLAGS=-I../windows/hunspell-1.3.3/include/hunspell

R_COMPILED_BY ?=-4.6.3
PKG_LIBS=-L../windows/hunspell-1.3.3/lib${subst gcc ,-,${R_COMPILED_BY}}${R_ARCH} -lhunspell-1.3

all: clean winlibs

winlibs:
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R"

clean:
	rm -f *.o *.so *.dll
